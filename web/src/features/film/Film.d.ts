// Types for the Astra Remotion composition (Film.jsx), vendored with npm `three` imports.
import type { ComponentType } from "react"

export declare const chapters: [start: number, end: number, eyebrow: string, title: string, body: string][]
export declare const Film: ComponentType
